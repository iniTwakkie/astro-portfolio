# Project notes

## Git workflow

- After making any changes to this repository, always commit them locally.
- Do **not** push to GitHub (origin `main`) until the user has reviewed the changes and explicitly approved the push.
- Cloudflare is connected to the GitHub repo and auto-deploys on every push to `main`, so a push is required to publish changes — which is exactly why pushes wait for review.